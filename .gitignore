.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Personal study data. Copy this file directly to move history between machines.
ultralearn.db
ultralearn.db-*

.streamlit/secrets.toml

# Frontend
node_modules/
desktop/dist/
desktop/.vite/

# Desktop shell (Tauri, when swapped in)
desktop/src-tauri/target/

.DS_Store

# Local UI dumps and Playwright shots. Screenshots can capture a live
# loopback token in the address bar, so this folder stays off git.
.ui-test/

# Secrets. API keys live in ~/.config/ultralearn/env (mode 600) or a local
# .env next to the repo — never in the tree.
.env
.env.*
!.env.example
credentials.json
secrets.json
*.pem
